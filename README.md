# Link Shortener
A simple and efficient link shortener service implemented in Node.js. 
This project allows you to create shortened URLs, making sharing of links easier. It includes a web interface to quickly generate short URLs from long ones.

## Technologies Used
- Node.js(express.js)
- ejs
- Bootstrap
- MongoDB
   
